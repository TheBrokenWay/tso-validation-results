"""
Analytics output version — include in every analytics JSON so future changes don't silently alter interpretation.
Bump when semantics or schema of any analytics output change.
"""
ANALYTICS_OUTPUT_VERSION = "1.0"
