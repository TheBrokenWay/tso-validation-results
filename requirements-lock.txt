# Predator X — Pinned Environment Snapshot
# Generated: 2026-02-15 from pip freeze
# Python: 3.13.11 (Miniconda3, Windows)
# Purpose: Reproducibility lock — exact versions known to pass 447/447 tests
#
# Core dependencies (declared in pyproject.toml):
#   rdkit==2025.9.3
#   numpy==2.3.5
#   requests (conda-managed)
#   reportlab (conda-managed)
#
# To recreate: pip install -r requirements-lock.txt
# Note: Some packages are conda-installed (@ file:// entries). For those,
# use conda install or install from PyPI equivalent.

numpy==2.3.5
rdkit==2025.9.3
reportlab>=4.0
requests>=2.31.0
scipy==1.16.3
setuptools==80.9.0
wheel==0.45.1
pytest==9.0.2
